## nodejs install
sudo apt-get install nodejs npm
sudo npm install -g n
sudo n lts
sudo apt-get purge nodejs npm
sudo apt-get autpremove

# n でインストールできるバージョン一覧
n ls

## git
user.name "dev01"
user.email "dev01@example.com"
